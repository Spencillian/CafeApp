import { View, Text, StyleSheet, SectionList, ActivityIndicator, SafeAreaView, ScrollView } from "react-native";
import * as React from 'react';

export default class Week extends React.Component{
    render(){
        return(
            <SafeAreaView>
                <Text>Week's Menus</Text>
            </SafeAreaView>
        );
    }
}